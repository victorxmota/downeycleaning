
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allows reading and writing of photos if the user is authenticated
    match /shifts/{allPaths=**} {
      allow read: if request.auth != null;
      allow write: if request.auth != null;
    }
  }
}
